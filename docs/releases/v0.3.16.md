# Template v0.3.16 — complete clone lifecycle proof

Stack remains pinned to v0.3.26 (`b2d235610691eead011cc738f278887a99e4bbe0`).
The new `check-abc` gate customizes ordinary project clones with linked task worktrees and ordinary/linked runtimes,
then releases v1.2.0 and v1.3.0 in dependency order with real local Git effects.
Build/repair and critic use explicit profiles in one task worktree; continuation
preserves the run and carries current critic evidence. Unmerged integration is
rejected without discarding the index or files; completion retry does not republish.

Native CLI doubles provide deterministic phase responses; no model calls occur.
The published v0.3.26 real Terra/Astra proof is linked and its 53 runtime file hashes
are checked separately. Existing pinned-stack failure tests cover lost push/publish
acknowledgements, unavailable profiles, unknown remotes, dirty cleanup and retries.

Local macOS/Linux candidate gates and fresh tag readback are required before task
completion. No GitHub Actions or production deployment. Rollback uses a new release
or prior immutable template/stack version; published tags are not rewritten.
