# v0.3.17 — guided first release and verified stack pairing

Pins stack v0.3.32, including compatibility release gates, exact-target upgrade
previews, static TOML versions, guided lifecycle settings and immutable release
pairings. The generated README block is checked against the annotated stack tag.

A fresh template copy can turn explicit answers into reviewed settings and a
first-task execution brief. The integration test uses the real planner, adoption,
intake, controller, local Git publication and readback to deliver v1.2.0. Its native
worker is a deterministic CLI double; it makes no model calls. Existing two-release,
resume, critic-repair and failure campaigns remain separate outer checks.

The starter selects no model, account or remote-write authority. Active source
maintenance records remain outside the published executable queue; completed proof
is preserved as non-executable history. Persistent-session/subagent adapters and
model escalation remain deferred.

Validation: current pairing/contract checks, guided first release, existing ABC
campaign and local clone checks on macOS and Linux. No GitHub Actions.
