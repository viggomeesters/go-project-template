{
  "schema": "go-workflow.repo-local.task.v1",
  "kind": "task",
  "execution_mode": "agent",
  "id": "advice-model-12-template-pin-v038",
  "project": "go-project-template",
  "status": "done",
  "summary": "Pin project template to immutable advice-to-outcome runtime v0.3.8",
  "description": "Update the template minimum runtime/ref after the verified local stack tag exists, then prove the normal no-override bootstrap path.",
  "scope": {
    "read": [
      ".go/project.json",
      "README.md"
    ],
    "modify": [
      ".go/project.json",
      "README.md"
    ]
  },
  "acceptance": [
    "Template requires stack version 0.3.8 and immutable ref v0.3.8.",
    "Full template check passes against the exact tagged stack without GO_STACK_ALLOW_DEV.",
    "The public pull path is release-ready; push remains explicitly unperformed."
  ],
  "verification": [
    "GO_STACK=../go-workflow-stack bash scripts/check.sh",
    "git diff --check"
  ],
  "claim": {
    "agent": "codex",
    "claimed_at": "2026-08-01T13:24:00+02:00"
  },
  "evidence": [
    {
      "created_at": "2026-08-01T13:24:24+02:00",
      "agent": "codex",
      "summary": "Template contract now requires 0.3.8 and immutable v0.3.8; local 0.3.8 development runtime validates it and the prior template recommendation smoke passed. Exact-tag no-override proof follows after the verified stack release commit/tag. No push or GitHub Actions. Self-reflect: release ordering requires the maintenance task to leave active state before template-check can clone the public smoke task."
    }
  ]
}
